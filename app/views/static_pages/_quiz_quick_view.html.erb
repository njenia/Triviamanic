<div>
  <% quiz.categories.each do |cat| %>
    <div class="pull-left">
      <div>
        <%= cat.name %>
      </div>
      <% cat.questions.each do |question| %>
        <div style="margin: 5px; border: black solid 1px; width: 20px; height: 10px; content: ''"></div>
      <% end %>
    </div>
  <% end %>
</div>

